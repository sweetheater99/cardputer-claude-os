.DS_Store
__pycache__/
*.pyc
*.pyo
*.log
.venv/
venv/
.env

# Local secrets / per-device configuration
buddy/device/apps/config.py
worker/.dev.vars
worker/.wrangler/

# MicroPython bytecode build artifacts (compile-on-push via mpy-cross,
# not source-of-truth — the .py is canonical).
*.mpy
